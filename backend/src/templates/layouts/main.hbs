<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <title>{{subject}}</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background: #f6f6f6;
        margin: 0;
        padding: 0;
      }
      .container {
        background: #fff;
        max-width: 600px;
        margin: 30px auto;
        padding: 32px 24px;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.07);
      }
      .footer {
        color: #888;
        font-size: 12px;
        margin-top: 32px;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="container">
      {{{body}}}
    </div>
    <div class="footer">
      &copy; {{year}} Ton Application. Tous droits réservés.
    </div>
  </body>
</html>